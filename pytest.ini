[pytest]
testpaths = cli/tests
markers =
    e2e: end-to-end tests against a live devnet (excluded by default, run with: pytest -m e2e)
; e2e tests are opt-in; a plain `pytest` run skips them,
; the command-line -m flag overrides this expression
addopts = -m "not e2e"
